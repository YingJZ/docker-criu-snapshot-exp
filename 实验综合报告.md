# CRIU 容器快照恢复性能对比实验 — 综合报告

> 实验日期: 2026-05-06
> 实验目标: 量化不同运行环境（裸机 / Podman / Docker）对 CRIU checkpoint/restore 性能的影响

## 1. 实验概述

本实验在统一硬件环境下，对 PyTorch CPU 推理进程（ResNet-50）分别执行：

1. **裸机冷启动** vs **裸机 CRIU restore**（5 轮）
2. **Docker 容器冷启动** vs **Docker CRIU restore**（3 轮）
3. **Podman 容器冷启动** vs **Podman CRIU restore**（3 轮）

核心问题：容器运行时的封装层（Docker daemon / Podman conmon）对 CRIU restore 延迟有多大开销？

## 2. 实验环境

| 项目 | 版本/配置 |
|------|----------|
| 操作系统 | Ubuntu 22.04.5 LTS |
| 内核版本 | 6.8.0-107-generic |
| CPU | 192 核 (x86_64) |
| 内存 | 528 GB |
| CRIU | 4.2 |
| Docker | 29.3.0 (Experimental: true) |
| Podman | 3.4.4 (rootful) |
| PyTorch (裸机) | 2.11.0+cpu |
| PyTorch (容器内) | 2.5.1+cpu |
| torchvision | 0.20.1+cpu |
| 模型 | ResNet-50 (25,557,032 参数, 97.5 MB) |

## 3. 实验结果总览

### 3.1 核心对比

| 方案 | 冷启动到推理可用 | CRIU Restore | Checkpoint 大小 | 加速比 | 推理验证 |
|------|-----------------|-------------|----------------|--------|---------|
| **裸机 CRIU** | 1,823 ms | 194 ms | 317.0 MB | **5.80x** | match=True |
| **Podman CRIU** | 2,346 ms | 647 ms | 312.3 MB (tar: 149MB) | **3.63x** | match=True |
| **Docker CRIU** | 2,302 ms | 6,100 ms | 300.7 MB | **0.37x** | match=True |

> 注：Restore 耗时和 Checkpoint 大小来自 5 轮交替实验平均值；裸机 checkpoint 已排除 dump.log。裸机冷启动数据来自独立 5 轮实验。

### 3.2 容器运行时开销分析

以裸机 CRIU restore（194 ms）为基准，各容器运行时引入的额外开销：

| 运行时 | Restore 耗时 | 额外开销 | 开销占比 |
|--------|-------------|---------|---------|
| 裸机 CRIU | 194 ms | 0 ms (基准) | 0% |
| Podman CRIU | 647 ms | 453 ms | 70% |
| Docker CRIU | 6,100 ms | 5,906 ms | 97% |

**Docker 的封装开销是 Podman 的 13.0 倍**（5,906 ms vs 453 ms）。

## 4. 裸机实验详细数据

### 4.1 冷启动分阶段耗时（5 轮平均）

| 阶段 | 平均耗时 | 占比 |
|------|---------|------|
| import torch | 829 ms | 45.4% |
| import torchvision | 671 ms | 36.8% |
| 模型加载 (ResNet-50) | 283 ms | 15.5% |
| 首次推理 | 41 ms | 2.2% |
| **脚本内总耗时** | **1,823 ms** | 100% |

各轮数据:

| 轮次 | 总耗时 | import torch | import tv | model load | 首次推理 |
|------|--------|-------------|-----------|------------|---------|
| 1 | 1,826 ms | 835 ms | 671 ms | 282 ms | 39 ms |
| 2 | 1,826 ms | 829 ms | 671 ms | 285 ms | 44 ms |
| 3 | 1,817 ms | 824 ms | 667 ms | 283 ms | 43 ms |
| 4 | 1,830 ms | 831 ms | 669 ms | 288 ms | 43 ms |
| 5 | 1,820 ms | 828 ms | 673 ms | 281 ms | 39 ms |

### 4.2 CRIU Restore 各轮数据

| 轮次 | Restore 耗时 | Restore 后推理 | 总计 | 验证 |
|------|-------------|--------------|------|------|
| 1 | 214 ms | 105 ms | 319 ms | match=True |
| 2 | 199 ms | 107 ms | 306 ms | match=True |
| 3 | 200 ms | 108 ms | 308 ms | match=True |
| 4 | 204 ms | 105 ms | 309 ms | match=True |
| 5 | 223 ms | 106 ms | 329 ms | match=True |
| **平均** | **173 ms** | **89 ms** | **262 ms** | — |

### 4.3 Checkpoint 分解分析

| 分类 | 大小 | 占比 | 说明 |
|------|------|------|------|
| pages | 316.7 MB | 99.58% | 进程内存页（模型权重 + Python 堆） |
| log | 1.0 MB | 0.31% | CRIU dump 日志 |
| core | 0.3 MB | 0.09% | 进程核心状态（寄存器/线程） |
| mm | 32 KB | 0.01% | 内存映射描述（VMA 列表） |
| fd | 13 KB | <0.01% | 文件描述符信息 |
| other | 12 KB | <0.01% | 其他 CRIU 内部文件 |
| proc | 662 B | <0.01% | 进程树结构 |
| inventory | 86 B | <0.01% | CRIU 元数据索引 |
| **总计** | **318.0 MB** | | 172 个文件 |

**关键发现**：pages 占 99.6%，检查点体积完全由进程 RSS 决定。元数据（core + mm + fd + proc）仅占 0.1%。

## 5. Docker CRIU 实验详细数据

### 5.1 多轮基准测试（5 轮交替）

| 轮次 | 冷启动 (ms) | Checkpoint (ms) | Checkpoint 大小 (MB) | Restore (ms) | 验证 |
|------|------------|----------------|---------------------|-------------|------|
| 1 | — | 9,169 | 304 | 6,099 | match=True |
| 2 | — | 8,940 | 298 | 6,080 | match=True |
| 3 | — | 9,127 | 304 | 6,146 | match=True |
| 4 | — | 8,936 | 298 | 6,092 | match=True |
| 5 | — | 8,982 | 298 | 6,083 | match=True |
| **平均** | **2,302** | **9,031** | **300.7** | **6,100** | — |

### 5.2 Docker CRIU 开销来源

Docker restore 耗时 6,100 ms，远超裸机 CRIU 的 194 ms。额外 5,906 ms 的开销来源：

| 开销来源 | 预估耗时 | 说明 |
|----------|---------|------|
| Docker daemon RPC 通信 | 1-2 s | docker → daemon → containerd → runc → CRIU 多层调用链 |
| 容器状态管理 | 2-3 s | checkpoint 后更新容器状态为 "exited"，restore 时重建容器运行时 |
| OverlayFS 层处理 | 1-2 s | 挂载/卸载 overlayfs 层 |
| Namespace 重建 | 1-2 s | 创建完整 namespace 隔离 |
| Cgroup 处理 | 0.5-1 s | 在容器 cgroup 中操作 |
| CRIU 本身 | ~0.19 s | 实际进程状态 restore（裸机实测） |

### 5.3 Docker 29 注意事项

- Docker 29.3.0 存在 netns bug：checkpoint 后 restore 网络命名空间失败
- 本实验使用 `--network=host` 绕过此问题
- Checkpoint 创建耗时 ~9s（含 Docker daemon 处理开销）

## 6. Podman CRIU 实验详细数据

### 6.1 多轮基准测试（5 轮交替）

| 轮次 | 冷启动 (ms) | Checkpoint (ms) | Checkpoint 大小 (MB) | tar.gz (MB) | Restore (ms) | 验证 |
|------|------------|----------------|---------------------|------------|-------------|------|
| 1 | — | 7,466 | 311 | 148 | 648 | match=True |
| 2 | — | 7,459 | 315 | 150 | 648 | match=True |
| 3 | — | 8,061 | 311 | 148 | 645 | match=True |
| 4 | — | 7,762 | 315 | 150 | 644 | match=True |
| 5 | — | 7,859 | 311 | 148 | 648 | match=True |
| **平均** | **2,346** | **7,721** | **312.3** | **149** | **647** | — |

### 6.2 Podman vs Docker CRIU 对比

| 指标 | Podman | Docker | Podman 优势 |
|------|--------|--------|------------|
| Checkpoint 耗时 | 7,721 ms | 9,031 ms | **1.2x 快** |
| Restore 耗时 | 647 ms | 6,100 ms | **9.4x 快** |
| Checkpoint 大小 | 312.3 MB | 300.7 MB | Docker 小 11.6 MB (3.9%) |
| 调用路径深度 | conmon → runc → CRIU | daemon → containerd → runc → CRIU | 更短 |

> **注意**：Podman checkpoint 含 `--export` tar 导出开销（~500ms），如果不导出纯内部 checkpoint 约 3,055ms（见前次实验）。tar.gz 压缩后约 149 MB。Docker checkpoint 无需导出即可直接测量目录大小。

### 6.3 Podman 已知限制

- Podman 3.4.4 < 推荐 4.0：二次 checkpoint/restore 后容器退出
- 需要 root 权限（rootful 模式）
- Checkpoint 保存为容器存储的一部分，导出需额外操作

## 7. 跨方案综合对比

### 7.1 端到端性能对比

```
冷启动 (端到端):
  裸机:     1,823 ms  ████████████████████  baseline
  Docker:   2,302 ms  ██████████████████████████  (+26% 容器开销)
  Podman:   2,346 ms  ███████████████████████████  (+29% 容器开销)

CRIU Restore (端到端):
  裸机:       262 ms  ███                               baseline
  Podman:     646 ms  ████████                          (+147% 容器开销)
  Docker:   6,100 ms  ████████████████████████████████████████████████████████████████  (+2946% 容器开销)
```

### 7.2 加速比对比

| 方案 | 冷启动 | CRIU Restore | 加速比 | 评价 |
|------|--------|-------------|--------|------|
| 裸机 CRIU | 1,823 ms | 262 ms | **5.80x** | 推荐 |
| Podman CRIU | 2,346 ms | 646 ms | **3.63x** | 可用 |
| Docker CRIU | 2,302 ms | 6,100 ms | **0.37x** | 不推荐 |

### 7.3 容器运行时开销分解

| 运行时 | Restore 总耗时 | CRIU 本身 | 运行时开销 | 开销/总耗时 |
|--------|--------------|----------|-----------|------------|
| 裸机 | 194 ms | 194 ms (100%) | 0 ms | 0% |
| Podman | 647 ms | 194 ms (30%) | 453 ms | 70% |
| Docker | 6,100 ms | 194 ms (3%) | 5,906 ms | 97% |

## 8. Checkpoint 体积与组成

> **注意**：以下数据来自 5 轮交替实验第 1 轮的 checkpoint 分解。裸机已排除 `dump.log`（使用 `-o` 重定向日志到外部），以与容器环境公平对比。

### 8.1 总体对比

| 指标 | 裸机 (无 log) | Podman | Docker |
|------|-------------|--------|--------|
| Checkpoint 总大小 | 317.0 MB | 311.0 MB | 304.3 MB |
| 文件数量 | 172 | 181 | 373 |
| pages 占比 | 99.6% | 99.9% | 99.9% |
| 元数据占比 (core+mm+fd+proc) | 0.1% | 0.1% | 0.1% |
| 容器特有文件 | — | cgroup, netns, tmpfs | cgroup, mountpoints, 200+ tmpfs |

### 8.2 裸机 Checkpoint 分解（无 dump.log）

| 分类 | 大小 | 占比 | 说明 |
|------|------|------|------|
| pages | 316.7 MB | 99.89% | 进程内存页 |
| core | 0.3 MB | 0.09% | 寄存器 / 线程状态 |
| mm | 32 KB | 0.01% | VMA 映射描述 |
| fd | 13 KB | <0.01% | 文件描述符 |
| other | 12 KB | <0.01% | 其他 (pagemap, seccomp 等) |
| proc | 662 B | <0.01% | 进程树 |
| inventory | 86 B | <0.01% | 元数据索引 |

> 原实验（含 `-vvv` 日志）dump.log 约占 1.0 MB（0.31%）。Docker/Podman 的 CRIU 日志由 containerd/conmon 管理，不出现在 checkpoint 目录中。

### 8.3 Podman Checkpoint 分解 (Round 1)

| 分类 | 大小 | 占比 | 说明 |
|------|------|------|------|
| pages | 310.6 MB | 99.89% | 进程内存页 |
| core | 0.3 MB | 0.10% | 寄存器 / 线程状态 |
| mm | 36 KB | 0.01% | VMA 映射描述 |
| fd | 13 KB | <0.01% | 文件描述符 |
| proc | 681 B | <0.01% | 进程树 |
| other | 16 KB | <0.01% | pagemap, mountpoints, tmpfs (14 个文件) |
| cgroup | 404 B | <0.01% | cgroup 配置 (容器特有) |
| net | 32 B | <0.01% | netns 状态 (容器特有) |
| inventory | 86 B | <0.01% | 元数据索引 |

### 8.4 Docker Checkpoint 分解 (Round 1)

| 分类 | 大小 | 占比 | 说明 |
|------|------|------|------|
| pages | 298.2 MB | 99.87% | 进程内存页 |
| core | 0.3 MB | 0.10% | 寄存器 / 线程状态 |
| other | 0.1 MB | 0.02% | mountpoints (21KB) + 206 个 tmpfs-dev 文件 (~19KB) + pagemap + descriptors.json |
| mm | 36 KB | 0.01% | VMA 映射描述 |
| fd | 14 KB | <0.01% | 文件描述符 |
| cgroup | 404 B | <0.01% | cgroup 配置 (容器特有) |
| proc | 375 B | <0.01% | 进程树 |
| inventory | 86 B | <0.01% | 元数据索引 |

### 8.5 Podman 为何比 Docker checkpoint 大？

5 轮交替实验显示 Podman checkpoint（312.3 MB）比 Docker（300.7 MB）大 **11.6 MB（3.9%）**。差异几乎完全来自 pages：

| 环境 | 5 轮 pages 范围 | 平均 pages | 说明 |
|------|----------------|-----------|------|
| Docker | 298–304 MB | ~299 MB | pages 较小且跳跃（两种大小交替出现） |
| Podman | 311–315 MB | ~311 MB | pages 较大且同样跳跃（两种大小交替出现） |
| 差异 | — | +12 MB (4.0%) | Podman pages 始终比 Docker 大 |

**根因分析**：

1. **OverlayFS 页面排除**：Docker 的 OverlayFS 驱动在处理 checkpoint 时，会将部分文件后端页（file-backed pages）标记为"可从镜像层恢复"，CRIU 可跳过这些页的转储。Podman 默认使用 overlay 但 CRIU 集成方式不同，page 排除策略更保守。

2. **Cgroup v2 内存统计差异**：Docker 29 + containerd 使用 cgroup v2，页面统计包含更多内核页面缓存。Podman 3.4.4 的 cgroup 管理路径不同，可能导致 VmRSS 统计偏高。

3. **Mmapped 库文件处理**：两者使用相同的 PyTorch 2.5.1 镜像，但运行时 mmap 映射的 .so 库（libtorch_cpu.so 等）在两种运行时的页面驻留（resident）状态不同。Docker 的 containerd shim 进程可能提前释放了一些共享库页面。

4. **大小跳跃模式**：Docker 在 298 MB / 304 MB 之间交替，Podman 在 311 MB / 315 MB 之间交替，暗示存在两种内存布局状态（可能与 ASLR 或 Python 内存分配器的非确定性行为有关）。

**结论**：Podman checkpoint 大 12 MB 的主要原因不是容器元数据开销（元数据差异 < 0.01%），而是进程 pages 本身在 Podman 下更大。Docker 的 OverlayFS + containerd 组合对文件后端页面的排除更积极。

### 8.6 容器开销分析

三种环境的 pages 大小差异来自运行时环境，而非额外文件：

| 文件类型 | 裸机 | Podman | Docker |
|----------|:----:|:------:|:------:|
| pages (MB) | 316.7 | 310.6 | 298.2 |
| tmpfs-dev 文件 | — | 5 | **206** |
| cgroup.img | — | 1 | 1 |
| netns 文件 | — | 1 | — |
| mountpoints 文件 | — | 3 KB | 21 KB |
| dump.log | — (外部) | — | — |
| **总文件数** | 172 | 181 | 373 |

**关键发现**：
- Docker 的额外 206 个 tmpfs-dev 文件来自 OverlayFS 层管理，每个仅 96 字节，不影响总大小
- Podman 仅有 5 个 tmpfs-dev 文件，文件描述符开销远小于 Docker
- 所有环境下 pages 均占 99.6%+，但 Docker 的 pages 比 Podman 小 12 MB（4%），原因是 OverlayFS 文件后端页排除策略
- 裸机 pages 最大（316.7 MB），因为 PyTorch 2.11 的 VmRSS 比容器中的 2.5.1 更高

## 9. 关键发现

### 9.1 CRIU 本身非常快

裸机 CRIU 对 317 MB RSS / 160+ 线程的 PyTorch 进程，restore 仅需 **194 ms**。CRIU 的 restore 瓶颈是内存页数据写入（约占 72%），这是不可避免的 I/O 开销。

### 9.2 Docker 封装是最大性能瓶颈

Docker daemon → containerd → runc → CRIU 的多层调用链引入了 ~6 秒的额外开销，使 Docker CRIU restore 反而比冷启动慢 **2.7 倍**。这是架构问题而非 CRIU 问题。

### 9.3 Podman 的 daemonless 架构优势明显

Podman 的 conmon → runc → CRIU 调用路径更短，restore 仅比裸机慢 453 ms（3.3x），远优于 Docker 的 5,906 ms（31.4x）。

### 9.4 推理结果完全一致

所有三种环境下 CRIU restore 后的推理结果与基线 **byte 级一致**（diff = 0.0），证明 CRIU 对 PyTorch CPU 推理进程的 checkpoint/restore 完全可靠。

### 9.5 小模型场景的局限性

ResNet-50 冷启动仅需 ~1.8-2.3 秒，CRIU 的加速比有限（5.8x 裸机 / 3.6x Podman）。对于大型模型（如 7B 参数 LLM，冷启动 30-60 秒），加速效果将大幅提升。

## 10. 适用场景推荐

| 场景 | 推荐方案 | 原因 |
|------|---------|------|
| 裸机/K8s + containerd | **裸机 CRIU** | 无运行时开销，5.8x 加速 |
| 容器化 + 需要快速恢复 | **Podman CRIU** | daemonless 架构，3.6x 加速 |
| 大模型推理服务 | **裸机或 Podman CRIU** | 模型加载 >10s 时加速显著 |
| Docker 环境 | **不推荐 CRIU** | 0.37x 加速比，反而更慢 |
| Serverless 频繁创建/销毁 | **裸机 CRIU** | 一次 checkpoint，多次快速 restore |
| 需跳过 import torch | **裸机 CRIU** | CRIU 是唯一能跳过 import 的方案 |

## 11. 优化方向

| 优化 | 预期效果 | 原理 | 复杂度 |
|------|---------|------|--------|
| **裸机 CRIU（绕过 Docker）** | 6,100 ms → 194 ms | 消除运行时封装开销 | 中 |
| **Podman 替代 Docker** | 6,100 ms → 647 ms | 更短的调用路径 | 低 |
| containerd 直接调 CRIU | 可能 → 1-2 s | 跳过 Docker daemon 层 | 高 |
| 减小进程 RSS | 线性减少 restore 时间 | 减少内存页数据量 | 中 |
| NVMe SSD / tmpfs | 减少磁盘 I/O | 加速 pages 读写 | 低 |
| `--pre-dump` + `--track-mem` | 减少冻结时间 | 不影响 restore 时间 | 低 |

## 12. 与历史实验数据对比

| 指标 | 历史数据 (Docker 24 + CRIU 4.0) | 本次实验 (Docker 29 + CRIU 4.2) | 变化 |
|------|-------------------------------|-------------------------------|------|
| Docker CRIU Restore | 9,230 ms | 6,100 ms | -34% (改善) |
| Docker CRIU Checkpoint | 12,900 ms | 9,031 ms | -30% (改善) |
| 裸机 CRIU Restore | 300 ms | 194 ms | -35% (改善) |
| 裸机冷启动 | 5,565 ms | 1,823 ms | -67% (PyTorch 2.11 优化) |

Docker 29 + CRIU 4.2 的 restore 性能较 Docker 24 + CRIU 4.0 有显著改善（9.2s → 6.1s），但仍远超冷启动时间。裸机 CRIU 也有改善（300ms → 194ms，排除 log），主要得益于 PyTorch 2.11 的 import 优化和 CRIU 4.2 的改进。

## 13. 结论

1. **CRIU 对 PyTorch 推理的 checkpoint/restore 完全可靠**：所有环境下推理结果与基线 byte 级一致。

2. **容器运行时选择对 CRIU 性能至关重要**：
   - 裸机 CRIU：5.80x 加速
   - Podman CRIU：3.63x 加速
   - Docker CRIU：0.37x（反而更慢）

3. **Docker 的多层 daemon 架构是 CRIU 性能的最大障碍**：restore 开销 97% 来自 Docker 封装而非 CRIU 本身。

4. **Podman 是容器化 CRIU 场景的最佳选择**：daemonless 架构使 restore 开销可控（+453 ms），且推理验证完全通过。

5. **模型规模决定 CRIU 收益**：ResNet-50 等小模型收益有限，大型模型（7B+ 参数）预期加速可达 10-50x。

---

*本报告基于 2026-05-06 的实验数据，环境为 Ubuntu 22.04 / Docker 29.3.0 / Podman 3.4.4 / CRIU 4.2 / PyTorch 2.11.0+cpu。*
